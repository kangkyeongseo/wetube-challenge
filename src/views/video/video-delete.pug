extends ../base

block content 
    if errorMessage
        span=errorMessage
    br
    span If you want to delete your Video, please enter your password.
    form(method="POST") 
        label(for="password") Password
        input(type="password" placeholder="Password" id="password" name="password" required )
        input(type="submit" value="Delete Video")