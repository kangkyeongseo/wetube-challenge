header
    ul  
        li
            a(href="/") Home
        if !loggedIn
            li 
                a(href="/login") Login
            li 
                a(href="/join") Join              
        if loggedIn
            li
                a(href="/video/upload") Upload Video        
            li 
                a(href=`/user/${loggedInUser._id}`)=loggedInUser.name
            li 
                a(href="/user/logout") Logout 
        