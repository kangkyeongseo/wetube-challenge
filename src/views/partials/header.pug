header
    ul  
        li
            a(href="/") Home
        if !logedIn
            li 
                a(href="/login") Login
            li 
                a(href="/join") Join              
        if logedIn
            li
                a(href="/video/upload") Upload Video        
            li 
                a(href=`/user/${logedInUser._id}`)=logedInUser.name
            li 
                a(href="/user/logout") Logout 
        